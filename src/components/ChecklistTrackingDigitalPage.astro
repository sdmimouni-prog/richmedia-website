---
import { CONTACT_FORM } from '../consts';

interface Props {
  lang?: 'fr' | 'en';
  downloadHref: string;
  downloadFilename: string;
}

const { lang = 'fr', downloadHref, downloadFilename } = Astro.props;
const isEnglish = lang === 'en';
const assetBase = '/assets/richmedia-tools/checklist-tracking-digital';

const t = isEnglish
  ? {
      eyebrow: 'Richmedia checklist',
      title: 'Pre-launch Digital Tracking Checklist',
      positioning: '30 checks before spending your first dirham on media.',
      subtitle:
        'Check that every click, lead and conversion can actually be measured before activating your media budget.',
      badge: '30 checks • Free PDF • Tracking & Data',
      primaryCta: 'Download the checklist',
      secondaryCta: 'View all tools',
      secondaryHref: '/en/tools/',
      heroAlt: 'Real PDF pages from the Richmedia pre-launch digital tracking checklist',
      previewEyebrow: 'Document preview',
      previewTitle: 'Real PDF pages from the checklist.',
      previewText: 'These visuals are generated from the exact PDF file used for download, without modifying its content.',
      controlsEyebrow: 'What it checks',
      controlsTitle: 'The critical tracking points to validate before launch.',
      keyMessage:
        'A campaign is not ready when the creative assets are ready. It is ready when measurement works end to end.',
      funnelEyebrow: 'Measurement chain',
      scoreEyebrow: 'Readiness score',
      scoreTitle: 'A simple indicator to decide whether the setup can go live.',
      scoreNote: 'This score is an educational indicator. It does not replace a technical, legal or security audit.',
      downloadEyebrow: 'Download',
      downloadTitle: 'Download the free PDF checklist',
      downloadText:
        'Fill in the form. After validation, the exact PDF file attached to this project starts downloading automatically.',
      submit: 'Download the free PDF checklist',
      loading: 'Validation in progress...',
      success: 'Your download has started. The PDF checklist is ready.',
      error: 'The download could not be started. Please try again.',
      trapSuccess: 'Download started. Thank you.',
      subject: '[Richmedia] Pre-launch Digital Tracking Checklist download',
      formName: 'Pre-launch Digital Tracking Checklist download',
      requestedResource: 'Pre-launch Digital Tracking Checklist — Richmedia',
      crmMarker: 'lead_download_pre_launch_digital_tracking_checklist',
      fields: {
        name: 'Full name *',
        email: 'Professional email *',
        company: 'Company *',
        job: 'Job title *',
      },
      fieldNames: {
        name: 'Full name',
        email: 'Professional email',
        company: 'Company',
        job: 'Job title',
      },
      previews: [
        { title: 'Cover & method', image: `${assetBase}/checklist-tracking-page-1.webp`, alt: 'Real first page of the Richmedia digital tracking checklist PDF' },
        { title: 'Analytics, GTM & conversions', image: `${assetBase}/checklist-tracking-page-2.webp`, alt: 'Real Analytics, GTM and conversions page from the Richmedia tracking checklist PDF' },
        { title: 'Media platforms & attribution', image: `${assetBase}/checklist-tracking-page-3.webp`, alt: 'Real media platforms and attribution page from the Richmedia tracking checklist PDF' },
        { title: 'Landing pages & forms', image: `${assetBase}/checklist-tracking-page-4.webp`, alt: 'Real landing pages and forms page from the Richmedia tracking checklist PDF' },
      ],
      controls: [
        '01 Analytics & GA4',
        '02 Google Tag Manager',
        '03 Events & conversions',
        '04 Meta Pixel & CAPI',
        '05 Google Ads',
        '06 TikTok & LinkedIn',
        '07 UTMs',
        '08 Landing pages & forms',
        '09 CRM & attribution',
        '10 WhatsApp & e-commerce',
        '11 Consent',
        '12 Pre-launch QA',
      ],
      funnel: ['CLICK', 'LANDING PAGE', 'LEAD', 'CRM', 'SALE', 'REPORTING'],
      scores: [
        { range: '90-100%', label: 'Ready to launch' },
        { range: '75-89%', label: 'Corrections recommended' },
        { range: '< 75%', label: 'Incomplete tracking' },
      ],
      relatedLinks: [
        { href: '/en/tools/', label: 'Tools & resources' },
        { href: '/en/tools/marketing-kpi-dashboard-template/', label: 'Marketing KPI Dashboard Template' },
        { href: '/en/tools/media-budget-leads-calculator/', label: 'Media Budget & Leads Calculator' },
        { href: '/en/expertises/web-si/', label: 'Web & systems' },
        { href: '/en/expertises/media-performance/', label: 'Media & performance' },
        { href: '/en/expertises/lead-generation-morocco/', label: 'Lead generation Morocco' },
      ],
      internalLabel: 'Related resources',
    }
  : {
      eyebrow: 'Checklist Richmedia',
      title: 'Checklist Tracking Digital avant lancement',
      positioning: '30 points à vérifier avant de dépenser votre premier dirham en média.',
      subtitle:
        'Vérifiez que chaque clic, lead et conversion pourra réellement être mesuré avant d’activer votre budget média.',
      badge: '30 contrôles • PDF gratuit • Tracking & Data',
      primaryCta: 'Télécharger la checklist',
      secondaryCta: 'Voir tous les outils',
      secondaryHref: '/outils/',
      heroAlt: 'Pages réelles du PDF Checklist Tracking Digital avant lancement Richmedia',
      previewEyebrow: 'Aperçu du document',
      previewTitle: 'Des pages réelles du PDF checklist.',
      previewText: 'Ces visuels sont générés depuis le fichier PDF exact proposé au téléchargement, sans modification de son contenu.',
      controlsEyebrow: 'Ce que la checklist contrôle',
      controlsTitle: 'Les points de tracking critiques à valider avant lancement.',
      keyMessage:
        'Une campagne n’est pas prête lorsque les créas sont prêtes. Elle est prête lorsque la mesure fonctionne de bout en bout.',
      funnelEyebrow: 'Chaîne de mesure',
      scoreEyebrow: 'Score de préparation',
      scoreTitle: 'Un indicateur simple pour décider si le setup peut partir en ligne.',
      scoreNote: 'Ce score est un indicateur pédagogique. Il ne remplace pas un audit technique, juridique ou sécurité.',
      downloadEyebrow: 'Téléchargement',
      downloadTitle: 'Télécharger gratuitement la checklist PDF',
      downloadText:
        'Remplissez le formulaire. Après validation, le fichier PDF exact joint au projet se télécharge automatiquement.',
      submit: 'Télécharger gratuitement la checklist PDF',
      loading: 'Validation en cours...',
      success: 'Votre téléchargement est lancé. La checklist PDF est prête.',
      error: "Le téléchargement n'a pas pu être lancé. Merci de réessayer.",
      trapSuccess: 'Téléchargement lancé. Merci.',
      subject: '[Richmedia] Téléchargement Checklist Tracking Digital avant lancement',
      formName: 'Téléchargement Checklist Tracking Digital avant lancement',
      requestedResource: 'Checklist Tracking Digital avant lancement — Richmedia',
      crmMarker: 'lead_download_checklist_tracking_digital_avant_lancement',
      fields: {
        name: 'Nom et prénom *',
        email: 'Email professionnel *',
        company: 'Entreprise *',
        job: 'Fonction *',
      },
      fieldNames: {
        name: 'Nom et prénom',
        email: 'Email professionnel',
        company: 'Entreprise',
        job: 'Fonction',
      },
      previews: [
        { title: 'Couverture & méthode', image: `${assetBase}/checklist-tracking-page-1.webp`, alt: 'Première page réelle du PDF checklist tracking digital Richmedia' },
        { title: 'Analytics, GTM & conversions', image: `${assetBase}/checklist-tracking-page-2.webp`, alt: 'Page réelle Analytics GTM et conversions du PDF checklist tracking Richmedia' },
        { title: 'Plateformes média & attribution', image: `${assetBase}/checklist-tracking-page-3.webp`, alt: 'Page réelle plateformes média et attribution du PDF checklist tracking Richmedia' },
        { title: 'Landing pages & formulaires', image: `${assetBase}/checklist-tracking-page-4.webp`, alt: 'Page réelle landing pages et formulaires du PDF checklist tracking Richmedia' },
      ],
      controls: [
        '01 Analytics & GA4',
        '02 Google Tag Manager',
        '03 Événements & conversions',
        '04 Meta Pixel & CAPI',
        '05 Google Ads',
        '06 TikTok & LinkedIn',
        '07 UTMs',
        '08 Landing pages & formulaires',
        '09 CRM & attribution',
        '10 WhatsApp & e-commerce',
        '11 Consentement',
        '12 QA avant lancement',
      ],
      funnel: ['CLIC', 'LANDING PAGE', 'LEAD', 'CRM', 'VENTE', 'REPORTING'],
      scores: [
        { range: '90-100 %', label: 'Ready to launch' },
        { range: '75-89 %', label: 'Corrections recommandées' },
        { range: '< 75 %', label: 'Tracking incomplet' },
      ],
      relatedLinks: [
        { href: '/outils/', label: 'Outils & ressources' },
        { href: '/outils/template-dashboard-kpi-marketing/', label: 'Template Dashboard KPI Marketing' },
        { href: '/outils/calculateur-budget-media-leads/', label: 'Calculateur Budget Média & Leads' },
        { href: '/expertises/web-si/', label: 'Web & SI' },
        { href: '/expertises/media-performance/', label: 'Média & performance' },
        { href: '/expertises/generation-leads-maroc/', label: 'Génération de leads Maroc' },
      ],
      internalLabel: 'Ressources liées',
    };
---

<div class="tracking-checklist-page" data-checklist-download-page>
  <section class="checklist-hero" aria-labelledby="tracking-checklist-title">
    <div class="hero-copy">
      <span class="section-kicker">{t.eyebrow}</span>
      <h1 id="tracking-checklist-title">{t.title}</h1>
      <p class="positioning">{t.positioning}</p>
      <p>{t.subtitle}</p>
      <span class="file-badge">{t.badge}</span>
      <div class="hero-actions">
        <a class="button button-primary" href="#download-checklist">
          {t.primaryCta}
          <i class="ph ph-download-simple" aria-hidden="true"></i>
        </a>
        <a class="button button-secondary" href={t.secondaryHref}>
          {t.secondaryCta}
          <i class="ph ph-arrow-up-right" aria-hidden="true"></i>
        </a>
      </div>
    </div>

    <figure class="hero-visual" aria-label={t.heroAlt}>
      <img
        class="hero-sheet hero-sheet-main"
        src={`${assetBase}/checklist-tracking-page-1.webp`}
        alt={t.heroAlt}
        width="1489"
        height="2105"
        loading="eager"
        decoding="async"
        fetchpriority="high"
      />
      <img
        class="hero-sheet hero-sheet-back"
        src={`${assetBase}/checklist-tracking-page-2.webp`}
        alt=""
        width="1489"
        height="2105"
        loading="eager"
        decoding="async"
        aria-hidden="true"
      />
    </figure>
  </section>

  <section class="preview-section" aria-labelledby="tracking-preview-title">
    <div class="section-heading">
      <span class="section-kicker">{t.previewEyebrow}</span>
      <h2 id="tracking-preview-title">{t.previewTitle}</h2>
      <p>{t.previewText}</p>
    </div>
    <div class="preview-grid">
      {t.previews.map((preview) => (
        <article class="preview-card">
          <figure>
            <img src={preview.image} alt={preview.alt} width="1489" height="2105" loading="lazy" decoding="async" />
          </figure>
          <h3>{preview.title}</h3>
        </article>
      ))}
    </div>
  </section>

  <section class="controls-section" aria-labelledby="tracking-controls-title">
    <div class="section-heading">
      <span class="section-kicker">{t.controlsEyebrow}</span>
      <h2 id="tracking-controls-title">{t.controlsTitle}</h2>
      <p>{t.keyMessage}</p>
    </div>
    <div class="control-grid">
      {t.controls.map((control) => (
        <article>{control}</article>
      ))}
    </div>
  </section>

  <section class="funnel-section" aria-labelledby="tracking-funnel-title">
    <div class="section-heading">
      <span class="section-kicker">{t.funnelEyebrow}</span>
      <h2 id="tracking-funnel-title">{t.keyMessage}</h2>
    </div>
    <div class="funnel-list">
      {t.funnel.map((step, index) => (
        <div class="funnel-step">
          <strong>{step}</strong>
          {index < t.funnel.length - 1 && <i class="ph ph-arrow-right" aria-hidden="true"></i>}
        </div>
      ))}
    </div>
    <nav class="internal-links" aria-label={t.internalLabel}>
      {t.relatedLinks.map((link) => (
        <a href={link.href}>{link.label}</a>
      ))}
    </nav>
  </section>

  <section class="score-section" aria-labelledby="tracking-score-title">
    <div class="section-heading">
      <span class="section-kicker">{t.scoreEyebrow}</span>
      <h2 id="tracking-score-title">{t.scoreTitle}</h2>
      <p>{t.scoreNote}</p>
    </div>
    <div class="score-grid">
      {t.scores.map((score) => (
        <article>
          <strong>{score.range}</strong>
          <span>{score.label}</span>
        </article>
      ))}
    </div>
  </section>

  <section class="download-section" id="download-checklist" aria-labelledby="tracking-download-title">
    <div class="download-copy">
      <span class="section-kicker">{t.downloadEyebrow}</span>
      <h2 id="tracking-download-title">{t.downloadTitle}</h2>
      <p>{t.downloadText}</p>
    </div>

    <form
      class="download-form"
      action={CONTACT_FORM.action}
      method="post"
      data-checklist-download-form
      data-download-url={downloadHref}
      data-contact-endpoint={CONTACT_FORM.ajaxAction}
      data-file-name={downloadFilename}
      data-subject={t.subject}
      data-email-field={t.fieldNames.email}
      data-loading-message={t.loading}
      data-success-message={t.success}
      data-error-message={t.error}
      data-trap-success-message={t.trapSuccess}
    >
      <input type="hidden" name="_form" value={t.formName} />
      <input type="hidden" name={isEnglish ? 'Requested resource' : 'Ressource demandée'} value={t.requestedResource} />
      <input type="hidden" name={isEnglish ? 'CRM preparation' : 'Préparation CRM'} value={t.crmMarker} />
      <input type="hidden" name="resource_slug" value="checklist-tracking-digital" />
      <input type="hidden" name="hubspot_ready" value="true" />
      <label class="form-honeypot" aria-hidden="true">
        Website
        <input type="text" name="company_url" tabindex="-1" autocomplete="off" />
      </label>
      <label>
        <span>{t.fields.name}</span>
        <input type="text" name={t.fieldNames.name} autocomplete="name" required />
      </label>
      <label>
        <span>{t.fields.email}</span>
        <input type="email" name={t.fieldNames.email} autocomplete="email" required />
      </label>
      <label>
        <span>{t.fields.company}</span>
        <input type="text" name={t.fieldNames.company} autocomplete="organization" required />
      </label>
      <label>
        <span>{t.fields.job}</span>
        <input type="text" name={t.fieldNames.job} autocomplete="organization-title" required />
      </label>
      <button type="submit">
        {t.submit}
        <i class="ph ph-download-simple" aria-hidden="true"></i>
      </button>
      <p class="form-status" data-form-status role="status" aria-live="polite"></p>
    </form>
  </section>
</div>

<script is:inline>
  (() => {
    const form = document.querySelector('[data-checklist-download-form]');
    if (!form) return;

    const status = form.querySelector('[data-form-status]');
    const submit = form.querySelector('button[type="submit"]');
    const setStatus = (state, message) => {
      status.textContent = message;
      status.dataset.state = state;
    };

    const triggerDownload = () => {
      const link = document.createElement('a');
      link.href = form.dataset.downloadUrl;
      link.download = form.dataset.fileName || '';
      document.body.appendChild(link);
      link.click();
      link.remove();
    };

    form.addEventListener('submit', async (event) => {
      event.preventDefault();

      if (!form.checkValidity()) {
        form.reportValidity();
        return;
      }

      const formData = new FormData(form);
      if (String(formData.get('company_url') || '').trim()) {
        form.reset();
        triggerDownload();
        setStatus('success', form.dataset.trapSuccessMessage || '');
        return;
      }

      const body = new URLSearchParams();
      formData.forEach((value, key) => body.append(key, String(value)));
      body.set('_page', window.location.href);
      body.set('_subject', form.dataset.subject || '');
      body.set('_template', 'table');
      body.set('_captcha', 'false');
      const emailField = form.dataset.emailField || '';
      const email = String(formData.get(emailField) || '').trim();
      if (email) body.set('_replyto', email);

      submit.disabled = true;
      submit.setAttribute('aria-busy', 'true');
      setStatus('loading', form.dataset.loadingMessage || '');

      try {
        const response = await fetch(form.dataset.contactEndpoint, {
          method: 'POST',
          headers: {
            Accept: 'application/json',
            'Content-Type': 'application/x-www-form-urlencoded;charset=UTF-8',
          },
          body,
        });
        const result = await response.json().catch(() => ({}));
        if (!response.ok || result.error || result.success === false || result.success === 'false') {
          throw new Error(result.message || form.dataset.errorMessage || '');
        }

        triggerDownload();
        form.reset();
        setStatus('success', form.dataset.successMessage || '');
      } catch (error) {
        setStatus('error', error.message || form.dataset.errorMessage || '');
      } finally {
        submit.disabled = false;
        submit.removeAttribute('aria-busy');
      }
    });
  })();
</script>

<style>
  :global(main) {
    width: 100%;
    max-width: none;
    padding: 0;
  }

  :global(html) {
    scroll-behavior: smooth;
  }

  .tracking-checklist-page {
    --purple: #7c3cff;
    --purple-soft: #aa7cff;
    --line: rgba(255, 255, 255, 0.13);
    --muted: rgba(255, 255, 255, 0.66);
    width: min(100% - 3rem, 1240px);
    margin: 0 auto;
    padding: clamp(7rem, 10vw, 9rem) 0 clamp(3rem, 6vw, 5.5rem);
    color: #fff;
    overflow-x: hidden;
  }

  .tracking-checklist-page,
  .tracking-checklist-page * {
    box-sizing: border-box;
  }

  .tracking-checklist-page::before {
    position: fixed;
    inset: 0;
    z-index: -1;
    background:
      radial-gradient(circle at 74% 8%, rgba(124, 60, 255, 0.15), transparent 27rem),
      radial-gradient(circle at 10% 30%, rgba(170, 124, 255, 0.08), transparent 22rem),
      linear-gradient(rgba(255, 255, 255, 0.022) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.022) 1px, transparent 1px);
    background-size: auto, auto, 84px 84px, 84px 84px;
    content: '';
    pointer-events: none;
  }

  h1,
  h2,
  h3,
  p {
    margin: 0;
  }

  .section-kicker {
    display: inline-flex;
    width: fit-content;
    align-items: center;
    gap: 0.65rem;
    color: var(--purple-soft);
    font-size: 0.74rem;
    font-weight: 900;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  .section-kicker::before {
    width: 2.1rem;
    height: 2px;
    background: linear-gradient(90deg, var(--purple), var(--purple-soft));
    content: '';
  }

  .checklist-hero {
    display: grid;
    grid-template-columns: minmax(0, 0.82fr) minmax(380px, 0.86fr);
    gap: clamp(1.4rem, 4vw, 4rem);
    align-items: center;
    min-height: 560px;
  }

  .hero-copy,
  .section-heading,
  .download-copy {
    display: grid;
    gap: 1rem;
  }

  .hero-copy h1 {
    max-width: 760px;
    font-size: clamp(3rem, 5.4vw, 5.1rem);
    font-weight: 900;
    line-height: 0.9;
    letter-spacing: 0;
  }

  .hero-copy p,
  .section-heading p,
  .download-copy p {
    max-width: 68ch;
    color: var(--muted);
    font-size: clamp(1rem, 1.3vw, 1.15rem);
    line-height: 1.62;
  }

  .hero-copy .positioning {
    max-width: 48ch;
    color: #fff;
    font-size: clamp(1.08rem, 1.55vw, 1.32rem);
    font-weight: 900;
    line-height: 1.32;
  }

  .file-badge {
    width: fit-content;
    border: 1px solid rgba(170, 124, 255, 0.38);
    border-radius: 999px;
    padding: 0.52rem 0.72rem;
    background: rgba(124, 60, 255, 0.12);
    color: #dfd3ff;
    font-size: 0.84rem;
    font-weight: 900;
  }

  .hero-actions,
  .internal-links {
    display: flex;
    flex-wrap: wrap;
    gap: 0.85rem;
  }

  .button,
  .download-form button {
    display: inline-flex;
    min-height: 3.15rem;
    align-items: center;
    justify-content: center;
    gap: 0.55rem;
    border: 1px solid transparent;
    border-radius: 7px;
    padding: 0 1.15rem;
    color: #fff;
    font: inherit;
    font-weight: 900;
    text-decoration: none;
    cursor: pointer;
    transition:
      transform 200ms ease,
      border-color 200ms ease,
      background 200ms ease,
      box-shadow 200ms ease;
  }

  .button:hover,
  .button:focus-visible,
  .download-form button:hover,
  .download-form button:focus-visible {
    transform: translateY(-2px);
  }

  .button-primary,
  .download-form button {
    background: linear-gradient(135deg, #9c5cff, #6729ef);
    box-shadow: 0 1.2rem 2.8rem rgba(124, 60, 255, 0.28);
  }

  .button-secondary {
    border-color: rgba(255, 255, 255, 0.18);
    background: rgba(255, 255, 255, 0.035);
  }

  .hero-visual,
  .preview-card,
  .controls-section,
  .funnel-section,
  .score-section,
  .download-section {
    border: 1px solid var(--line);
    border-radius: 8px;
    background:
      radial-gradient(circle at 16% 0%, rgba(124, 60, 255, 0.13), transparent 14rem),
      linear-gradient(145deg, rgba(20, 21, 31, 0.92), rgba(7, 8, 12, 0.92));
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.045);
  }

  .hero-visual {
    position: relative;
    display: grid;
    min-height: 520px;
    margin: 0;
    place-items: center;
    overflow: hidden;
    background:
      radial-gradient(circle at 55% 44%, rgba(124, 60, 255, 0.18), transparent 16rem),
      #05060a;
    box-shadow: 0 2rem 5.5rem rgba(0, 0, 0, 0.34);
  }

  .hero-sheet {
    display: block;
    width: min(70%, 330px);
    height: auto;
    border-radius: 5px;
    background: #fff;
    box-shadow: 0 1.5rem 3.8rem rgba(0, 0, 0, 0.5);
    transition:
      transform 260ms ease,
      box-shadow 260ms ease;
  }

  .hero-sheet-main {
    position: relative;
    z-index: 2;
    transform: rotate(-4deg);
  }

  .hero-sheet-back {
    position: absolute;
    z-index: 1;
    transform: translate(4.4rem, 1rem) rotate(5deg) scale(0.92);
    opacity: 0.55;
    filter: saturate(0.85);
  }

  @media (hover: hover) {
    .hero-visual:hover .hero-sheet-main {
      transform: translateY(-8px) rotate(-2deg) scale(1.025);
      box-shadow: 0 2rem 4.8rem rgba(0, 0, 0, 0.58);
    }

    .hero-visual:hover .hero-sheet-back {
      transform: translate(5rem, 0.5rem) rotate(7deg) scale(0.92);
    }
  }

  .preview-section,
  .controls-section,
  .funnel-section,
  .score-section,
  .download-section {
    margin-top: clamp(1.7rem, 4vw, 3.4rem);
  }

  .preview-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 0.85rem;
    margin-top: 1rem;
  }

  .preview-card {
    display: grid;
    gap: 0.85rem;
    padding: 0.85rem;
    transition:
      transform 220ms ease,
      border-color 220ms ease,
      box-shadow 220ms ease;
  }

  .preview-card:hover {
    transform: translateY(-4px);
    border-color: rgba(170, 124, 255, 0.56);
    box-shadow: 0 1.2rem 3rem rgba(75, 38, 180, 0.18);
  }

  .preview-card figure {
    display: grid;
    overflow: hidden;
    min-height: 17rem;
    margin: 0;
    place-items: center;
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 8px;
    background: #fff;
  }

  .preview-card img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: top;
  }

  .preview-card h3 {
    font-size: 1rem;
    line-height: 1.16;
  }

  .controls-section,
  .funnel-section,
  .score-section,
  .download-section {
    padding: clamp(1.15rem, 3vw, 2rem);
  }

  .control-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 0.72rem;
    margin-top: 1rem;
  }

  .control-grid article {
    min-height: 4.7rem;
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 8px;
    padding: 0.9rem;
    background: rgba(255, 255, 255, 0.035);
    color: #e7defe;
    font-weight: 900;
    line-height: 1.24;
    transition:
      transform 200ms ease,
      border-color 200ms ease,
      background 200ms ease;
  }

  .control-grid article:hover {
    transform: translateY(-3px);
    border-color: rgba(170, 124, 255, 0.5);
    background: rgba(124, 60, 255, 0.1);
  }

  .funnel-list,
  .score-grid {
    display: grid;
    gap: 0.7rem;
    margin-top: 1rem;
  }

  .funnel-list {
    grid-template-columns: repeat(6, minmax(0, 1fr));
  }

  .funnel-step {
    display: grid;
    gap: 0.42rem;
    place-items: center;
    min-height: 5.4rem;
    border: 1px solid rgba(170, 124, 255, 0.26);
    border-radius: 8px;
    background: rgba(124, 60, 255, 0.11);
    text-align: center;
  }

  .funnel-step strong {
    font-size: 0.8rem;
    font-weight: 950;
    letter-spacing: 0.1em;
  }

  .funnel-step i {
    color: var(--purple-soft);
    font-size: 1.1rem;
  }

  .score-grid {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }

  .score-grid article {
    display: grid;
    gap: 0.6rem;
    min-height: 8rem;
    align-content: center;
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 8px;
    padding: 1rem;
    background: rgba(255, 255, 255, 0.035);
  }

  .score-grid strong {
    color: #fff;
    font-size: clamp(2rem, 4vw, 3.5rem);
    line-height: 0.95;
  }

  .score-grid span {
    color: var(--muted);
    font-weight: 900;
  }

  .internal-links {
    margin-top: 1rem;
  }

  .internal-links a {
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 999px;
    padding: 0.5rem 0.7rem;
    color: rgba(255, 255, 255, 0.74);
    font-size: 0.82rem;
    font-weight: 900;
    text-decoration: none;
  }

  .internal-links a:hover,
  .internal-links a:focus-visible {
    border-color: rgba(170, 124, 255, 0.62);
    color: #fff;
  }

  .download-section {
    display: grid;
    grid-template-columns: minmax(0, 0.8fr) minmax(360px, 0.62fr);
    gap: clamp(1.2rem, 3vw, 2rem);
    align-items: start;
    scroll-margin-top: 7rem;
  }

  .download-copy h2,
  .section-heading h2 {
    font-size: clamp(2rem, 4vw, 4rem);
    line-height: 0.98;
    letter-spacing: 0;
  }

  .download-form {
    display: grid;
    gap: 0.85rem;
  }

  .download-form label {
    display: grid;
    gap: 0.45rem;
  }

  .download-form span {
    color: rgba(255, 255, 255, 0.72);
    font-size: 0.84rem;
    font-weight: 900;
  }

  .download-form input {
    min-width: 0;
    min-height: 3rem;
    border: 1px solid rgba(255, 255, 255, 0.14);
    border-radius: 7px;
    padding: 0 0.9rem;
    background: rgba(255, 255, 255, 0.045);
    color: #fff;
    font: inherit;
  }

  .download-form input:focus {
    border-color: rgba(170, 124, 255, 0.72);
    outline: 2px solid rgba(124, 60, 255, 0.24);
    outline-offset: 0;
  }

  .download-form button {
    width: 100%;
    margin-top: 0.15rem;
  }

  .download-form button:disabled {
    cursor: wait;
    opacity: 0.72;
  }

  .form-honeypot {
    position: absolute;
    left: -999rem;
    width: 1px;
    height: 1px;
    overflow: hidden;
  }

  .form-status {
    min-height: 1.25rem;
    color: rgba(255, 255, 255, 0.68);
    font-size: 0.88rem;
    font-weight: 800;
  }

  .form-status[data-state='success'] {
    color: #73e38a;
  }

  .form-status[data-state='error'] {
    color: #ff8f8f;
  }

  @media (prefers-reduced-motion: reduce) {
    .button,
    .download-form button,
    .hero-sheet,
    .preview-card,
    .control-grid article {
      transition: none;
    }
  }

  @media (max-width: 1120px) {
    .checklist-hero,
    .download-section {
      grid-template-columns: 1fr;
    }

    .preview-grid,
    .control-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .funnel-list {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }
  }

  @media (max-width: 720px) {
    .tracking-checklist-page {
      width: min(100% - 1rem, 1240px);
      padding-top: 6.2rem;
    }

    .checklist-hero {
      min-height: 0;
    }

    .hero-copy h1 {
      font-size: clamp(2.45rem, 12vw, 3.45rem);
    }

    .hero-actions,
    .button {
      width: 100%;
    }

    .hero-visual {
      min-height: 430px;
    }

    .hero-sheet {
      width: min(68%, 270px);
    }

    .hero-sheet-back {
      transform: translate(2.3rem, 1rem) rotate(5deg) scale(0.92);
    }

    .preview-grid,
    .control-grid,
    .funnel-list,
    .score-grid {
      grid-template-columns: 1fr;
    }

    .preview-card figure {
      min-height: 18rem;
    }

    .download-copy h2,
    .section-heading h2 {
      font-size: clamp(2rem, 10vw, 2.8rem);
    }

    .download-section,
    .controls-section,
    .funnel-section,
    .score-section {
      padding: 1rem;
    }
  }
</style>
