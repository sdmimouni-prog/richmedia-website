---
import BaseLayout from '../layouts/BaseLayout.astro';
import { breadcrumbs, localBusiness } from '../lib/schema';
import { SITE } from '../consts';

const pageUrl = `${SITE.url}/carrieres`;
const teamBase = '/assets/richmedia-home/team-carousel';
const agencyBase = '/assets/richmedia-agence';

const schema = [
  localBusiness(),
  breadcrumbs([
    { name: 'Accueil', url: SITE.url },
    { name: 'Carrières', url: pageUrl },
  ]),
];

const heroPeople = [
  { image: 'salah.webp', name: 'Salah Eddine MIMOUNI' },
  { image: 'amal.webp', name: 'Amal Amazouz' },
  { image: 'tarik.webp', name: 'Tarik El Abbadi' },
  { image: 'oussama.webp', name: 'Oussama TAHTAH' },
  { image: 'halima-felloui.webp', name: 'FELLIOUI Halima' },
  { image: 'oumaima-lassri.webp', name: 'Oumaima LASSRI' },
  { image: 'elhoussine-bw.webp', name: 'Elhoussine Essmami' },
  { image: 'ikram-bw.webp', name: 'Ikram Dehiri' },
  { image: 'safwane.webp', name: 'Safwane MOUSTAINE' },
  { image: 'malak-billaore.webp', name: 'Malak BILLAORE' },
  { image: 'rachid-oualla.webp?v=black-d71ec18', name: 'Rachid OUALLA' },
  { image: 'rachid.webp', name: 'Rachid Hlalma' },
  { image: 'abdelkader.webp', name: 'Abdelkader El Bouhaddouti' },
  { image: 'youssef.webp', name: 'Youssef Amazouz' },
  { image: 'wail.webp', name: 'Wail El Asri' },
  { image: 'ghita-mimouni.webp', name: 'Ghita MIMOUNI' },
  { image: 'marwane.webp', name: 'Marwane Jouay', className: 'portrait-focus-low' },
  { image: 'soukaina-sammouni.webp', name: 'Soukaina SAMMOUNI' },
  { image: 'zakaria.webp', name: 'Zakaria Mouchtati' },
  { image: 'zouhair-bw.webp', name: 'Zouhair Lyzouli' },
  { image: 'yasmine-ouazzani-touhamy.webp?v=black-yasmine-ab487fe', name: 'Yasmine Ouazzani Touhamy' },
  { image: 'wafaa.webp', name: 'Yassmine Boudial' },
  { image: 'ayoub.webp', name: 'Ayoub Es-Samlali' },
  { image: 'yassine.webp', name: 'Mohamed Yassine Dargal' },
];
const heroGridPeople = heroPeople.slice(0, 19);

const poles = [
  {
    icon: 'ph ph-target',
    title: 'Stratégie',
    text: 'Positionnement, cadrage, architecture de campagne, SEO/GEO, parcours et indicateurs business.',
  },
  {
    icon: 'ph ph-palette',
    title: 'Création',
    text: 'Direction artistique, brand content, social media, vidéo, motion design et expériences créatives.',
  },
  {
    icon: 'ph ph-chart-bar',
    title: 'Performance',
    text: 'Achat média, tracking, analytics, génération de leads, CRM, WhatsApp et optimisation continue.',
  },
  {
    icon: 'ph ph-code',
    title: 'Technologie',
    text: 'Sites web, plateformes, automatisation, IA, intégrations et systèmes connectés pour scaler.',
  },
];

const stats = [
  { icon: 'ph ph-users-three', value: '25', label: 'profils', text: 'stratégie, création, média, tech et production' },
  { icon: 'ph ph-squares-four', value: '8', label: 'unités', text: 'métiers spécialisés et connectés' },
  { icon: 'ph ph-map-pin', value: '4', label: 'bureaux', text: 'Casablanca, Dubaï, Montréal, Strasbourg' },
  { icon: 'ph ph-chart-line-up', value: '+156', label: 'projets', text: 'réalisés pour des marques et institutions' },
];

const culture = [
  {
    icon: 'ph ph-users-three',
    title: 'Collaboration',
    text: 'L’intelligence collective au service des meilleures idées.',
  },
  {
    icon: 'ph ph-lightbulb',
    title: 'Créativité',
    text: 'Nous encourageons les idées nouvelles et l’audace créative.',
  },
  {
    icon: 'ph ph-sparkle',
    title: 'Autonomie',
    text: 'Des responsabilités réelles, la liberté d’agir et d’innover.',
  },
  {
    icon: 'ph ph-graduation-cap',
    title: 'Apprentissage',
    text: 'La curiosité est notre moteur, l’apprentissage est continu.',
  },
];

const team = [
  { image: 'salah.webp', name: 'Salah-Eddine Mimouni', role: 'CEO & CTO', group: 'Direction' },
  { image: 'amal.webp', name: 'Amal Amazouz', role: 'COO & CDO', group: 'Direction' },
  { image: 'tarik.webp', name: 'Tarik El Abbadi', role: 'Directeur Communication & Event', group: 'Direction' },
  { image: 'elhoussine-bw.webp', name: 'Elhoussine Essmami', role: 'Strategic Planner', group: 'Stratégie' },
  { image: 'ikram-bw.webp', name: 'Ikram Dehiri', role: 'Senior Account Manager', group: 'Account management' },
  { image: 'oussama.webp', name: 'Oussama TAHTAH', role: 'Senior Account Manager', group: 'Account management' },
  { image: 'oumaima-lassri.webp', name: 'Oumaima LASSRI', role: 'Senior Account Manager', group: 'Account management' },
  { image: 'halima-felloui.webp', name: 'FELLIOUI Halima', role: 'Account Manager', group: 'Account management' },
  { image: 'soukaina-sammouni.webp', name: 'Soukaina SAMMOUNI', role: 'Account Manager', group: 'Account management' },
  { image: 'yasmine-ouazzani-touhamy.webp?v=black-yasmine-ab487fe', name: 'Yasmine Ouazzani Touhamy', role: 'Account Manager', group: 'Account management' },
  { image: 'zouhair-bw.webp', name: 'Zouhair Lyzouli', role: 'Senior Full Stack Developer', group: 'Développement' },
  { image: 'zakaria.webp', name: 'Zakaria Mouchtati', role: 'Senior Full Stack Developer', group: 'Développement' },
  { image: 'yassmine.webp?v=black-wafaa-1', name: 'Wafae Lamsabni', role: 'Project Manager', group: 'Gestion de projet' },
  { image: 'yassine.webp', name: 'Mohamed Yassine Dargal', role: 'Project Manager', group: 'Gestion de projet' },
  { image: 'ayoub.webp', name: 'Ayoub Es-Samlali', role: 'Project Manager', group: 'Gestion de projet' },
  { image: 'wafaa.webp', name: 'Yassmine Boudial', role: 'Project Manager', group: 'Gestion de projet' },
  { image: 'safwane.webp', name: 'Safwane MOUSTAINE', role: 'Trafic Manager senior', group: 'Marketing' },
  { image: 'malak-billaore.webp', name: 'Malak BILLAORE', role: 'Responsable administrative', group: 'Administration' },
  { image: 'rachid-oualla.webp?v=black-d71ec18', name: 'Rachid OUALLA', role: 'Directeur artistique', group: 'Création' },
  { image: 'rachid.webp', name: 'Rachid Hlalma', role: 'Art Director', group: 'Création' },
  { image: 'abdelkader.webp', name: 'Abdelkader El Bouhaddouti', role: 'Graphic Final Artist', group: 'Design' },
  { image: 'youssef.webp', name: 'Youssef Amazouz', role: 'Graphic Final Artist', group: 'Design' },
  { image: 'wail.webp', name: 'Wail El Asri', role: 'Graphic Final Artist', group: 'Design' },
  { image: 'ghita-mimouni.webp', name: 'Ghita MIMOUNI', role: 'Junior Design Scout', group: 'Design' },
  { image: 'marwane.webp', name: 'Marwane Jouay', role: 'Réalisateur & monteur vidéo', group: 'Production' },
];
---

<BaseLayout
  title="Carrières agence digitale à Casablanca | Richmedia"
  description="Rejoignez Richmedia à Casablanca : stratégie digitale, création, média, SEO/GEO, web, CRM, WhatsApp, IA et production."
  canonical={pageUrl}
  schema={schema}
>
  <div class="team-page">
    <section class="team-hero" aria-labelledby="team-title">
      <div class="hero-copy">
        <span class="section-kicker">Équipe Richmedia</span>
        <h1 id="team-title">Construire ensemble ce qui rend les marques <strong>visibles.</strong></h1>
        <p>
          À Casablanca, Richmedia rassemble des profils créatifs, analytiques, média et techniques autour d’un même
          objectif : transformer les idées en dispositifs digitaux utiles, visibles et mesurables pour les marques au
          Maroc et à l’international.
        </p>
        <a class="round-link" href="#profils">
          Découvrir l’équipe
          <span><i class="ph ph-arrow-right" aria-hidden="true"></i></span>
        </a>
      </div>

      <div class="hero-portraits" aria-label="Portraits de l’équipe Richmedia">
        {heroGridPeople.map((person) => (
          <figure class:list={['hero-portrait', person.className]}>
            <img
              src={`${teamBase}/${person.image}`}
              alt={person.name}
              width="420"
              height="520"
              loading="eager"
              decoding="async"
            />
            <figcaption>{person.name}</figcaption>
          </figure>
        ))}
        <a class="hero-portrait hero-join-card" href="/contact" aria-label="Join us">
          <span class="join-plus" aria-hidden="true">+</span>
          <span class="join-label">Join us</span>
        </a>
      </div>
    </section>

    <section class="why-team" aria-labelledby="why-title">
      <h2 id="why-title">Nos pôles d’expertise</h2>
      <div class="why-grid">
        {poles.map((reason) => (
          <article>
            <i class={reason.icon} aria-hidden="true"></i>
            <h3>{reason.title}</h3>
            <p>{reason.text}</p>
          </article>
        ))}
      </div>
    </section>

    <section class="team-stats" aria-label="Chiffres clés équipe Richmedia">
      {stats.map((stat) => (
        <article>
          <i class={stat.icon} aria-hidden="true"></i>
          <div>
            <strong>{stat.value}</strong>
            <span>{stat.label}</span>
            <p>{stat.text}</p>
          </div>
        </article>
      ))}
    </section>

    <section class="team-life" aria-labelledby="life-title">
      <div class="life-media">
        <img
          src={`${agencyBase}/agence-studio-meeting.webp`}
          alt="Vie chez Richmedia, équipe digitale en atelier à Casablanca"
          width="1280"
          height="720"
          loading="lazy"
          decoding="async"
        />
        <a class="play-button" href="/agence" aria-label="Découvrir la culture Richmedia">
          <i class="ph ph-monitor-play" aria-hidden="true"></i>
        </a>
        <p>Plus qu’un lieu de travail, un espace pour grandir, collaborer et créer ensemble.</p>
      </div>
      <div class="life-copy">
        <span class="section-kicker">La vie chez Richmedia</span>
        <h2 id="life-title">Une équipe qui connecte stratégie, création, média et technologie.</h2>
        <p>
          Nos équipes accompagnent des campagnes de notoriété, performance, social media, SEO/GEO, sites web,
          WhatsApp, CRM et automatisation IA. Chaque profil apporte une expertise, mais le résultat naît de la collaboration.
        </p>
        <div class="culture-grid">
          {culture.map((item) => (
            <article>
              <i class={item.icon} aria-hidden="true"></i>
              <h3>{item.title}</h3>
              <p>{item.text}</p>
            </article>
          ))}
        </div>
        <a class="text-link" href="/agence">
          Découvrir notre culture
          <i class="ph ph-arrow-right" aria-hidden="true"></i>
        </a>
      </div>
    </section>

    <section class="open-jobs team-roster" id="profils" aria-label="Équipe Richmedia">
      <div class="section-row">
        <a href="/contact">Parler à l’équipe <i class="ph ph-arrow-right" aria-hidden="true"></i></a>
      </div>
      <div class="members-grid">
        {team.map((member, index) => (
          <article>
            <div class="member-image">
              <img
                src={`${teamBase}/${member.image}`}
                alt={member.name}
                width="420"
                height="520"
                loading={index < 4 ? 'eager' : 'lazy'}
                decoding="async"
              />
            </div>
            <div class="member-info">
              <span>{member.group}</span>
              <h3>{member.name}</h3>
              <p>{member.role}</p>
            </div>
          </article>
        ))}
      </div>
    </section>

    <section class="team-cta" aria-labelledby="cta-title">
      <div>
        <h2 id="cta-title">Une équipe senior peut changer la vitesse d’un projet.</h2>
        <p>Échangeons sur vos objectifs de croissance digitale au Maroc et à l’international.</p>
      </div>
      <a class="button button-primary" href="/contact">
        Parler à l’équipe
        <i class="ph ph-arrow-up-right" aria-hidden="true"></i>
      </a>
    </section>
  </div>
</BaseLayout>

<style>
  :global(main) {
    width: 100%;
    max-width: none;
    margin: 0;
    padding: 0;
  }

  .team-page {
    --container: min(100% - 3rem, 1320px);
    --purple: #7c3cff;
    --purple-soft: #a56bff;
    --panel: rgba(11, 12, 18, 0.78);
    --line: rgba(255, 255, 255, 0.15);
    --muted: rgba(255, 255, 255, 0.68);
    overflow: hidden;
    background:
      radial-gradient(circle at 16% 7%, rgba(124, 60, 255, 0.28), transparent 28rem),
      radial-gradient(circle at 84% 22%, rgba(124, 60, 255, 0.18), transparent 30rem),
      linear-gradient(180deg, #030408 0%, #050608 52%, #030408 100%);
    color: #fff;
  }

  .team-page::before {
    position: fixed;
    inset: 0;
    z-index: 0;
    background-image:
      linear-gradient(rgba(255, 255, 255, 0.026) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.018) 1px, transparent 1px);
    background-size: 64px 64px;
    content: "";
    pointer-events: none;
  }

  .team-hero,
  .why-team,
  .team-stats,
  .team-life,
  .open-jobs,
  .recruitment,
  .team-cta {
    position: relative;
    z-index: 1;
    width: var(--container);
    margin-inline: auto;
  }

  .team-hero {
    display: grid;
    grid-template-columns: minmax(0, 0.82fr) minmax(560px, 1.18fr);
    gap: clamp(2rem, 5vw, 6rem);
    align-items: center;
    margin-top: 30px;
    min-height: auto;
    padding: clamp(3.25rem, 6vw, 6rem) 0 2.75rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  }

  .section-kicker,
  .why-team h2,
  .section-row h2,
  .recruitment h2 {
    letter-spacing: 0.18em;
    text-transform: uppercase;
  }

  .section-kicker {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    color: var(--purple-soft);
    font-size: 1.0625rem;
    font-weight: 800;
  }

  .section-kicker::before {
    width: 0.48rem;
    height: 0.48rem;
    border-radius: 999px;
    background: currentColor;
    box-shadow: 0 0 24px rgba(165, 107, 255, 0.76);
    content: "";
  }

  .hero-copy h1 {
    max-width: 640px;
    margin: 1.3rem 0 1.2rem;
    font-size: clamp(3.4rem, 7.4vw, 7.2rem);
    line-height: 0.95;
    letter-spacing: 0;
  }

  .hero-copy h1 strong {
    display: block;
    color: var(--purple);
  }

  .hero-copy p {
    max-width: 520px;
    margin: 0 0 2rem;
    color: var(--muted);
    font-size: 1.0625rem;
    line-height: 1.78;
  }

  .round-link {
    display: inline-flex;
    align-items: center;
    gap: 1rem;
    padding: 0.62rem 0.65rem 0.62rem 1.35rem;
    border: 1px solid var(--line);
    border-radius: 999px;
    color: #fff;
    font-size: 1.0625rem;
    font-weight: 800;
    text-decoration: none;
    transition: transform 220ms ease, border-color 220ms ease, box-shadow 220ms ease;
  }

  .round-link span {
    display: grid;
    width: 3rem;
    height: 3rem;
    place-items: center;
    border-radius: 999px;
    background: linear-gradient(135deg, #8c48ff, #6e20f4);
    box-shadow: 0 0 32px rgba(124, 60, 255, 0.78);
  }

  .round-link:hover {
    transform: translateY(-3px);
    border-color: rgba(165, 107, 255, 0.65);
    box-shadow: 0 24px 80px rgba(124, 60, 255, 0.2);
  }

  .hero-portraits {
    position: relative;
    display: grid;
    grid-template-columns: repeat(5, minmax(0, 1fr));
    grid-auto-rows: clamp(88px, 7.2vw, 118px);
    gap: 5px;
    align-self: center;
    min-height: 0;
    padding: 5px;
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 12px;
    background:
      linear-gradient(145deg, rgba(124, 60, 255, 0.12), transparent 34%),
      rgba(255, 255, 255, 0.028);
    box-shadow: 0 28px 90px rgba(0, 0, 0, 0.42);
    isolation: isolate;
  }

  .hero-portraits::before {
    position: absolute;
    inset: 0;
    z-index: 0;
    border-radius: inherit;
    background: radial-gradient(circle at 50% 42%, rgba(124, 60, 255, 0.16), transparent 48%);
    opacity: 0.9;
    content: "";
    pointer-events: none;
  }

  .hero-portrait {
    position: relative;
    z-index: 1;
    overflow: hidden;
    margin: 0;
    min-width: 0;
    border: 1px solid rgba(124, 60, 255, 0.22);
    border-radius: 4px;
    background:
      radial-gradient(circle at 50% 45%, rgba(124, 60, 255, 0.12), transparent 58%),
      #08090f;
    box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.025);
    transition: transform 260ms ease, border-color 260ms ease, box-shadow 260ms ease, filter 260ms ease;
  }

  .hero-portrait:hover {
    z-index: 10;
    transform: translateY(-6px) scale(1.045);
    border-color: rgba(165, 107, 255, 0.82);
    box-shadow: 0 18px 54px rgba(124, 60, 255, 0.3);
  }

  .hero-portrait img {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: contain;
    object-position: center;
    filter: grayscale(1) contrast(1.08);
    transform: scale(0.94);
    transition: filter 300ms ease, transform 300ms ease;
  }

  .hero-portrait:hover img {
    filter: grayscale(0.18) saturate(1.08) contrast(1.04);
    transform: scale(0.99);
  }

  .hero-portrait figcaption {
    position: absolute;
    right: 0.5rem;
    bottom: 0.45rem;
    left: 0.5rem;
    z-index: 2;
    color: #fff;
    font-size: 0.72rem;
    font-weight: 900;
    letter-spacing: 0.02em;
    line-height: 1.06;
    opacity: 0;
    transform: translateY(0.35rem);
    transition: opacity 240ms ease, transform 240ms ease;
  }

  .hero-portrait::after {
    position: absolute;
    inset: auto 0 0;
    height: 58%;
    background: linear-gradient(180deg, transparent, rgba(0, 0, 0, 0.78));
    opacity: 0;
    transition: opacity 240ms ease;
    content: "";
  }

  .hero-portrait:hover::after,
  .hero-portrait:hover figcaption {
    opacity: 1;
    transform: translateY(0);
  }

  .portrait-focus-low img {
    object-position: center;
    transform: scale(0.94);
  }

  .portrait-focus-low:hover img {
    transform: scale(0.99);
  }

  .hero-join-card {
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    gap: 0.5rem;
    color: #fff;
    text-decoration: none;
    background:
      radial-gradient(circle at 50% 22%, rgba(165, 107, 255, 0.36), transparent 48%),
      linear-gradient(145deg, rgba(124, 60, 255, 0.2), rgba(255, 255, 255, 0.035));
  }

  .hero-join-card::after {
    inset: 0;
    height: auto;
    background: linear-gradient(180deg, rgba(255, 255, 255, 0.04), rgba(124, 60, 255, 0.18));
    opacity: 0.34;
  }

  .join-plus {
    position: relative;
    z-index: 2;
    color: #a56bff;
    font-size: clamp(3.25rem, 5.3vw, 5.2rem);
    font-weight: 300;
    line-height: 0.72;
    text-shadow: 0 0 28px rgba(124, 60, 255, 0.64);
    transition: color 260ms ease, transform 260ms ease;
  }

  .join-label {
    position: relative;
    z-index: 2;
    color: rgba(255, 255, 255, 0.88);
    font-size: 0.78rem;
    font-weight: 900;
    letter-spacing: 0.18em;
    line-height: 1;
    text-transform: uppercase;
    transition: color 260ms ease, transform 260ms ease;
  }

  .hero-join-card:hover .join-plus {
    color: #fff;
    transform: rotate(90deg) scale(1.08);
  }

  .hero-join-card:hover .join-label {
    color: #c5a4ff;
    transform: translateY(-0.1rem);
  }

  .why-team,
  .open-jobs,
  .recruitment {
    padding: clamp(2.6rem, 5vw, 4.5rem) 0 0;
  }

  .why-team h2,
  .section-row h2,
  .recruitment h2 {
    margin: 0 0 1.45rem;
    font-size: 1.0625rem;
  }

  .why-grid,
  .jobs-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 1.35rem;
  }

  .why-grid article,
  .jobs-grid article,
  .culture-grid article {
    position: relative;
    overflow: hidden;
    border: 1px solid var(--line);
    border-radius: 12px;
    background: linear-gradient(145deg, rgba(17, 18, 27, 0.9), rgba(8, 9, 14, 0.76));
    transition: transform 260ms ease, border-color 260ms ease, background 260ms ease, box-shadow 260ms ease;
  }

  .why-grid article::before,
  .jobs-grid article::before,
  .culture-grid article::before {
    position: absolute;
    inset: auto 0 0;
    height: 42%;
    background: radial-gradient(circle at 50% 100%, rgba(124, 60, 255, 0.32), transparent 68%);
    opacity: 0;
    transition: opacity 260ms ease;
    content: "";
  }

  .why-grid article {
    min-height: 230px;
    padding: 2rem 1.7rem;
  }

  .why-grid article:hover,
  .jobs-grid article:hover,
  .culture-grid article:hover {
    transform: translateY(-8px);
    border-color: rgba(165, 107, 255, 0.7);
    background: linear-gradient(145deg, rgba(26, 21, 42, 0.96), rgba(9, 10, 16, 0.82));
    box-shadow: 0 24px 70px rgba(124, 60, 255, 0.22);
  }

  .why-grid article:hover::before,
  .jobs-grid article:hover::before,
  .culture-grid article:hover::before {
    opacity: 1;
  }

  .why-grid i,
  .culture-grid i {
    display: block;
    margin-bottom: 1.8rem;
    color: var(--purple-soft);
    font-size: 2.7rem;
    filter: drop-shadow(0 0 18px rgba(165, 107, 255, 0.45));
  }

  .why-grid h3,
  .culture-grid h3,
  .jobs-grid h3,
  .process-line h3 {
    margin: 0 0 0.85rem;
    font-size: 1.25rem;
    line-height: 1.18;
  }

  .why-grid p,
  .culture-grid p,
  .jobs-grid p,
  .process-line p {
    margin: 0;
    color: var(--muted);
    line-height: 1.65;
  }

  .team-stats {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    margin-top: 1.6rem;
    border: 1px solid var(--line);
    border-radius: 12px;
    background: rgba(12, 13, 20, 0.8);
    box-shadow: 0 20px 80px rgba(0, 0, 0, 0.28);
  }

  .team-stats article {
    position: relative;
    display: flex;
    gap: 1.2rem;
    align-items: center;
    overflow: hidden;
    min-height: 112px;
    padding: 1.35rem 1.6rem;
    border-right: 1px solid rgba(255, 255, 255, 0.1);
    background: rgba(255, 255, 255, 0);
    transition: background 280ms ease, box-shadow 280ms ease;
  }

  .team-stats article::before {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(110deg, transparent 0%, rgba(165, 107, 255, 0.18) 45%, transparent 72%),
      radial-gradient(circle at 20% 50%, rgba(124, 60, 255, 0.2), transparent 58%);
    content: "";
    opacity: 0;
    transform: translateX(-80%);
    transition: opacity 280ms ease, transform 560ms cubic-bezier(0.19, 1, 0.22, 1);
    pointer-events: none;
  }

  .team-stats article:hover {
    background: rgba(124, 60, 255, 0.08);
    box-shadow: inset 0 0 0 1px rgba(165, 107, 255, 0.28), 0 0 2rem rgba(124, 60, 255, 0.14);
  }

  .team-stats article:hover::before {
    opacity: 1;
    transform: translateX(0);
  }

  .team-stats article:last-child {
    border-right: 0;
  }

  .team-stats i {
    position: relative;
    z-index: 1;
    color: var(--purple-soft);
    font-size: 2.25rem;
    transition: color 260ms ease, transform 320ms ease, filter 320ms ease;
  }

  .team-stats strong {
    position: relative;
    z-index: 1;
    display: inline-block;
    margin-right: 0.3rem;
    font-size: 2.45rem;
    line-height: 1;
    transition: color 260ms ease, transform 320ms ease;
  }

  .team-stats span {
    position: relative;
    z-index: 1;
    color: #fff;
    font-weight: 800;
  }

  .team-stats p {
    position: relative;
    z-index: 1;
    margin: 0.25rem 0 0;
    color: var(--muted);
    font-size: 1.0625rem;
  }

  .team-stats article:hover i {
    color: #fff;
    filter: drop-shadow(0 0 14px rgba(165, 107, 255, 0.75));
    transform: translateY(-2px) rotate(-5deg) scale(1.08);
  }

  .team-stats article:hover strong {
    color: #fff;
    transform: translateY(-2px);
  }

  .team-life {
    display: grid;
    grid-template-columns: minmax(0, 0.95fr) minmax(0, 1fr);
    gap: clamp(2rem, 5vw, 5rem);
    align-items: center;
    padding: clamp(3.5rem, 7vw, 6rem) 0 0;
  }

  .life-media {
    position: relative;
    overflow: hidden;
    min-height: 360px;
    border: 1px solid var(--line);
    border-radius: 14px;
    background: #08090f;
    box-shadow: 0 1.6rem 4rem rgba(0, 0, 0, 0.25);
    transition: border-color 320ms ease, box-shadow 320ms ease, transform 320ms ease;
  }

  .life-media::before {
    position: absolute;
    inset: 0;
    z-index: 1;
    background:
      linear-gradient(115deg, transparent 0%, rgba(255, 255, 255, 0.14) 42%, transparent 62%),
      radial-gradient(circle at 50% 45%, rgba(124, 60, 255, 0.2), transparent 58%);
    content: "";
    opacity: 0;
    transform: translateX(-70%);
    transition: opacity 320ms ease, transform 680ms cubic-bezier(0.19, 1, 0.22, 1);
    pointer-events: none;
  }

  .life-media::after {
    position: absolute;
    inset: 0;
    z-index: 1;
    background: linear-gradient(180deg, transparent 44%, rgba(0, 0, 0, 0.78) 100%);
    content: "";
    transition: background 320ms ease;
    pointer-events: none;
  }

  .life-media img {
    display: block;
    width: 100%;
    height: 100%;
    min-height: 360px;
    object-fit: cover;
    transition: filter 420ms ease, transform 620ms ease;
  }

  .life-media:hover,
  .life-media:focus-within {
    border-color: rgba(165, 107, 255, 0.64);
    box-shadow: 0 2rem 5rem rgba(0, 0, 0, 0.34), 0 0 2.4rem rgba(124, 60, 255, 0.18);
    transform: translateY(-6px);
  }

  .life-media:hover::before,
  .life-media:focus-within::before {
    opacity: 1;
    transform: translateX(0);
  }

  .life-media:hover::after,
  .life-media:focus-within::after {
    background: linear-gradient(180deg, rgba(124, 60, 255, 0.08), rgba(0, 0, 0, 0.84) 100%);
  }

  .life-media:hover img,
  .life-media:focus-within img {
    filter: saturate(1.08) contrast(1.04);
    transform: scale(1.035);
  }

  .play-button {
    position: absolute;
    top: 50%;
    left: 50%;
    z-index: 2;
    display: grid;
    width: 4.4rem;
    height: 4.4rem;
    place-items: center;
    border: 1px solid rgba(255, 255, 255, 0.25);
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.16);
    color: #fff;
    font-size: 1.8rem;
    text-decoration: none;
    transform: translate(-50%, -50%);
    transition: transform 220ms ease, background 220ms ease, box-shadow 220ms ease;
    backdrop-filter: blur(10px);
  }

  .play-button:hover {
    background: rgba(124, 60, 255, 0.72);
    box-shadow: 0 0 46px rgba(124, 60, 255, 0.5);
    transform: translate(-50%, -50%) scale(1.08);
  }

  .life-media:hover .play-button,
  .life-media:focus-within .play-button {
    background: rgba(124, 60, 255, 0.78);
    box-shadow: 0 0 42px rgba(124, 60, 255, 0.48);
    transform: translate(-50%, -50%) scale(1.08);
  }

  .life-media p {
    position: absolute;
    right: 1.35rem;
    bottom: 1.25rem;
    left: 1.35rem;
    z-index: 2;
    margin: 0;
    font-size: 1.08rem;
    line-height: 1.55;
  }

  .life-copy h2 {
    max-width: 620px;
    margin: 1rem 0 1rem;
    font-size: clamp(2rem, 4vw, 3.5rem);
    line-height: 1.02;
  }

  .life-copy > p {
    max-width: 670px;
    margin: 0 0 1.6rem;
    color: var(--muted);
    font-size: 1.0625rem;
    line-height: 1.75;
  }

  .culture-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    border-top: 1px solid rgba(255, 255, 255, 0.08);
    border-left: 1px solid rgba(255, 255, 255, 0.08);
  }

  .culture-grid article {
    min-height: 170px;
    padding: 1.5rem;
    border-radius: 0;
    border-top: 0;
    border-left: 0;
  }

  .culture-grid i {
    margin-bottom: 1rem;
    font-size: 2rem;
  }

  .text-link,
  .section-row a,
  .jobs-grid a {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    color: var(--purple-soft);
    font-weight: 800;
    text-decoration: none;
  }

  .text-link {
    margin-top: 1.35rem;
  }

  .section-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    margin-bottom: 1.35rem;
  }

  .jobs-grid {
    grid-template-columns: repeat(6, minmax(0, 1fr));
  }

  .jobs-grid article {
    min-height: 230px;
    padding: 1.35rem;
  }

  .jobs-grid span {
    display: grid;
    width: 3rem;
    height: 3rem;
    margin-bottom: 1.35rem;
    place-items: center;
    border-radius: 999px;
    background: rgba(124, 60, 255, 0.18);
    color: var(--purple-soft);
    font-size: 1.35rem;
  }

  .jobs-grid small {
    display: block;
    margin: 0.25rem 0 1.5rem;
    color: var(--muted);
  }

  .team-roster {
    scroll-margin-top: 7rem;
  }

  .members-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 1.15rem;
  }

  .members-grid article {
    position: relative;
    overflow: hidden;
    min-height: 360px;
    border: 1px solid var(--line);
    border-radius: 12px;
    background: linear-gradient(145deg, rgba(17, 18, 27, 0.92), rgba(8, 9, 14, 0.78));
    transition: transform 260ms ease, border-color 260ms ease, box-shadow 260ms ease;
  }

  .members-grid article::after {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(180deg, transparent 35%, rgba(0, 0, 0, 0.88) 88%),
      radial-gradient(circle at 50% 100%, rgba(124, 60, 255, 0.24), transparent 62%);
    content: "";
    pointer-events: none;
  }

  .members-grid article:hover {
    transform: translateY(-8px);
    border-color: rgba(165, 107, 255, 0.72);
    box-shadow: 0 28px 80px rgba(124, 60, 255, 0.2);
  }

  .member-image {
    position: absolute;
    inset: 0;
  }

  .member-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center top;
    filter: grayscale(0.18) saturate(1.05) contrast(1.04);
    transform: scale(1.01);
    transition: transform 420ms ease, filter 420ms ease;
  }

  .members-grid article:hover .member-image img {
    filter: grayscale(0) saturate(1.12) contrast(1.06);
    transform: scale(1.07);
  }

  .member-info {
    position: absolute;
    right: 1.2rem;
    bottom: 1.2rem;
    left: 1.2rem;
    z-index: 2;
  }

  .member-info span {
    display: inline-flex;
    margin-bottom: 0.55rem;
    color: var(--purple-soft);
    font-size: 0.8125rem;
    font-weight: 900;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .member-info h3 {
    margin: 0;
    font-size: 0.875rem;
    line-height: 1.12;
  }

  .member-info p {
    margin: 0.45rem 0 0;
    color: rgba(255, 255, 255, 0.72);
    font-size: 0.875rem;
    font-weight: 700;
    line-height: 1.28;
  }

  .recruitment {
    padding-bottom: 0;
  }

  .process-line {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 2rem;
    border-top: 1px solid rgba(165, 107, 255, 0.35);
    padding-top: 1.4rem;
  }

  .process-line article {
    display: flex;
    gap: 1rem;
    align-items: flex-start;
    position: relative;
  }

  .process-line article:not(:last-child)::after {
    position: absolute;
    top: 1.45rem;
    right: -1.7rem;
    width: 1.4rem;
    height: 1px;
    background: repeating-linear-gradient(90deg, rgba(165, 107, 255, 0.8), rgba(165, 107, 255, 0.8) 3px, transparent 3px, transparent 7px);
    content: "";
  }

  .process-line span {
    display: grid;
    flex: 0 0 auto;
    width: 3rem;
    height: 3rem;
    place-items: center;
    border: 2px solid var(--purple-soft);
    border-radius: 999px;
    background: rgba(124, 60, 255, 0.14);
    color: #fff;
    font-size: 1.25rem;
    font-weight: 900;
    box-shadow: 0 0 28px rgba(124, 60, 255, 0.28);
  }

  .team-cta {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto auto;
    gap: 1.5rem;
    align-items: center;
    margin-top: 3.2rem;
    margin-bottom: 4rem;
    padding: 2rem 3rem;
    border: 1px solid rgba(165, 107, 255, 0.52);
    border-radius: 14px;
    background:
      radial-gradient(circle at 25% 100%, rgba(124, 60, 255, 0.44), transparent 32rem),
      linear-gradient(135deg, rgba(35, 20, 69, 0.96), rgba(12, 10, 20, 0.92));
    box-shadow: 0 22px 90px rgba(124, 60, 255, 0.2);
  }

  .team-cta h2 {
    margin: 0 0 0.4rem;
    font-size: clamp(1.9rem, 4vw, 3rem);
    line-height: 1.08;
  }

  .team-cta p {
    margin: 0;
    color: rgba(255, 255, 255, 0.78);
  }

  .hand-note {
    color: var(--purple-soft);
    font-size: 1.1rem;
    font-weight: 900;
    transform: rotate(-7deg);
  }

  .button.button-primary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.65rem;
    min-height: 3.8rem;
    padding: 0.9rem 1.8rem;
    border-radius: 8px;
    background: linear-gradient(135deg, #8b49ff, #641df1);
    color: #fff;
    font-weight: 900;
    text-decoration: none;
    box-shadow: 0 18px 60px rgba(124, 60, 255, 0.34);
    transition: transform 220ms ease, box-shadow 220ms ease;
  }

  .button.button-primary:hover {
    transform: translateY(-3px);
    box-shadow: 0 24px 72px rgba(124, 60, 255, 0.46);
  }

  @media (max-width: 1180px) {
    .team-hero,
    .team-life {
      grid-template-columns: 1fr;
    }

    .hero-portraits {
      grid-template-columns: repeat(5, minmax(0, 1fr));
      grid-auto-rows: clamp(76px, 10vw, 96px);
    }

    .why-grid,
    .team-stats,
    .jobs-grid,
    .members-grid,
    .process-line {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .team-stats article:nth-child(2n) {
      border-right: 0;
    }

    .team-stats article:nth-child(n + 3) {
      border-top: 1px solid rgba(255, 255, 255, 0.1);
    }

    .process-line article::after {
      display: none;
    }

    .team-cta {
      grid-template-columns: 1fr;
    }

    .hand-note {
      transform: none;
    }
  }

  @media (max-width: 720px) {
    .team-page {
      --container: min(100% - 1.25rem, 1320px);
    }

    .team-hero {
      min-height: auto;
      padding-top: 3rem;
    }

    .hero-copy h1 {
      font-size: clamp(3rem, 16vw, 4.7rem);
    }

    .hero-copy p,
    .life-copy > p {
      font-size: 1.0625rem;
    }

    .hero-portraits {
      grid-template-columns: repeat(4, minmax(0, 1fr));
      grid-auto-rows: clamp(68px, 18vw, 92px);
      gap: 4px;
      padding: 4px;
    }

    .hero-portrait:hover {
      transform: none;
    }

    .hero-portrait figcaption,
    .hero-portrait::after {
      display: none;
    }

    .why-grid,
    .team-stats,
    .team-life,
    .culture-grid,
    .jobs-grid,
    .members-grid,
    .process-line {
      grid-template-columns: 1fr;
    }

    .team-stats article {
      border-right: 0;
      border-top: 1px solid rgba(255, 255, 255, 0.1);
    }

    .team-stats article:first-child {
      border-top: 0;
    }

    .section-row {
      align-items: flex-start;
      flex-direction: column;
    }

    .team-cta {
      padding: 1.45rem;
    }
  }
</style>
